global:
  resolve_timeout: 10m
route:
  group_by: ["job"]
  group_wait: 30
  group_interval: 2m
  repeat_interval: 10m
  receiver: "slack-notifications"
  routes:
    - match:
        alertname: Watchdog
    - match:
        app: amo
receivers:
  - name: "slack-notifications"
    webhook_configs:
      - url: https://fb0425ab4c65.ngrok.io/api/sendPrometheusAlertsToSlack
        send_resolved: true
